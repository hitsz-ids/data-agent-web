import React, { useEffect } from 'react';

import styles from './index.less.module.less';

const ConnectionCreate: React.FC = () => {
  useEffect(() => {
    // Fetch data or perform any side effects here
  }, []);

  return <div className={styles.connectionCreate}></div>;
};

export default ConnectionCreate;
